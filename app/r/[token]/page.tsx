import {getAdminSupabase} from '@/lib/supabase-admin'; import LifeWheelClient from './ui';
export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params;const a=getAdminSupabase();const {data}=await a.from('life_wheels').select('id,token,status,used_at,started_at,people(full_name)').eq('token',token).maybeSingle();if(!data)return <main className="container"><div className="card"><h1>Link inválido</h1></div></main>;if(data.status==='completed'||data.used_at)return <main className="container"><div className="card"><h1>Roda da Vida concluída</h1><p>Este link já foi utilizado.</p></div></main>;if(!data.started_at)await a.from('life_wheels').update({status:'started',started_at:new Date().toISOString()}).eq('id',data.id).eq('status','pending');return <LifeWheelClient wheel={data as any}/>}
